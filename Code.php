I swear , he is so fucking talented. 


It displays the following error : nó hiển thi các lỗi sau
Thanks for your interest in our products.
If I see anything that needs to be addressed I’ll get back to you.
Thank you for all of your help!

i am in the process of setting up
How can we accomplish this ? lam the nao chung toi co the thuc hien dieu do
Thank you in advance,       cam on ban truoc

please help me to clarify matters.
to be clear.

I'm so sorry for late response, I had one vacation travel. Today, I went back to work and will assist you.

I'm not mistaken
You can pay for the customization service via this link:
http://www.magestore.com/magento-customization-service.html
Please note in there your detailed customization requirements. After your payment is complete, please provide me here with your FTP and Admin credentials so I can process the work for you in the shortest time.
Hope to hear from you soon! 

Thank you. When ready in a week or 2 we will be in toich.

what I got is a reprimand from you  || những gì tôi nhận được là lời khiển trách từ bạn:

What am I supposed to do now? toi phai lam gi bay gio
ownership : quyen so huu
any way ! here you are ! // dù sao! của bạn đây !

My apologies for the delay.

Yes, I still need assistance.
I did update the ticket with new control panel details – 
I’m currently organizing the ftp access for you.
I’ll be in touch very soon

we have rectified this issue!
THanks for assistance, Karl.

I have received your reply, then I really quite busy recently and might miss your message via skype : duong.diep52

here is the simple but powerful rule.
always give people more than they expect to get.

I should inform you, Our company has a policy of​ Customization :
Now, I just support for customer, I can't to do customize for customer, because we had Customize team and you must pay for them.
+ 30$ for 1 hours to work on magento 1.
+ 40$ for 1 hours to work on magento 2.
if you ok, you can send details for me, then I will contact with them to estimate time and fee, then I will inform you to get your confirm.


Yes, you are welcome.
If you have any problem, feel free to let me know, I will support you.

I noticed you left a message here. Could you please state your question here? I'm always happy to help :)
Wait for a moment, we are trying to reach out the problem is. I'll get back to you soon then

msVietnam20!06@2016#$

weird, I allowed already.
it should be accessible.
try again, and if it's not possible,
I'll find the other way.
but for now, you may not need ftp.
I'll check another issue if there is.

Thank you, could you ask them, will I be able to distinguish orders from website and WEBPOS in QuickBook?

I asked and was informed that they will integrate Quickbook with Inventory and webpos.

Thank you for your confirmation. I am glad to help.
Please allow me to mark this ticket as solved.

I look forward to your news.

It seems that some fields in database are missing during the installation of inventory.
I found that "product_location" field is missing during installation of Inventory, 

Thus it has no records in this adjustment.

Sorry for the problems you encountered by our products,

the problems that you are facing


They said, you should remove  unnecessary menu items at the moment.
They will try to improve for next version and sent apologies ​for the issue that you encountered.


I'm sorry to trouble you too, 
bother you too,
did you mind if I 
https://magestore.zendesk.com/agent/filters/85711787 - team 1
https://magestore.zendesk.com/agent/filters/85711897 - team 2
9749
magestore_support2 / support@123
https://docs.google.com/spreadsheets/d/1h-4Fkcfhgcnu9CgsQuSJ1rgxB9LkFNIiOEuXdcMBoKI/edit#gid=0

It seems that there was a weird ftp access problem


1. please download and extract it.
2. merge this file to Magento root folder.
3. Flush cache.
4. Please follow with this video guide below to refer how to use this tool :
a) reset on-hold qty https://drive.google.com/file/d/0B90hrFQe0LX2QU1FaUxvbUN3WlE/view
b) correct wrong qty: https://drive.google.com/file/d/0B90hrFQe0LX2TzZjU3hPNVRVNHM/view

Please install it on dev site to test before apply it on live site.

https://github.com/magento-hackathon/Hackathon_MageMonitoring

We greatly appreciate it!
Thanks, and have a great day ahead!



Let's take a look on the following example: Let's imagine  : hãy nhìn vào ví dụ sau đây : hãy tưởng tượng
 Roughly saying : nói chung 


due to some malfunction in Magento2, 
all other brands were removed in the attributed... 
I did not notice, nor expected that to happen


<?php
tìm file rỗng : 
find . -type f -name '*.php' -empty


core_config_data table in phpMyAdmin.
PATH                         VALUE
web/unsecure/base_url        http://www.example.com/
web/unsecure/base_link_url   {{unsecure_base_url}}
web/unsecure/base_skin_url   {{unsecure_base_url}}skin/
web/unsecure/base_media_url  {{unsecure_base_url}}media/
web/unsecure/base_js_url     {{unsecure_base_url}}js/


// Setup Cron on server : 
	 /usr/local/bin/php -f /home/dosscap1/public_html/cron.php >> /home/dosscap1/public_html/var/log/cron.log 2>&1 
	
	
/*
Warning : PDOStatement:execute():MYSQL server has gone away in .... \library\Zend\DB\Statement\PDO.php on line 228
*/
=>  Increase the max_allowed_packet
/*	
Fatal error: Maximum execution time of 300 seconds exceeded in C:\inetpub\wwwroot\FuturePrint\lib\internal\mPdf\classes\otl.php on line 5312	
*/
=>  Increase the max_execution_time
	
SET FOREIGN_KEY_CHECKS=0 ;
DELETE FROM `erp_inventory_supplier`;
ALTER TABLE `erp_inventory_supplier` AUTO_INCREMENT = 1;
	




return $collection_of_things = new Varien_Data_Collection();




	
if( $importData['color'] && $product->getResource()->getAttribute('color')->getSource()->getOptionId($importData['color']))
$product->setData('color', $product->getResource()->getAttribute("color")->getSource()->getOptionId($importData['color']));


$customOptions = Zend_Json::decode($customOptions); //php : decode 
                 json_encode($array);

js:    var notify = transport.responseText;
       var json = JSON.parse(notify);
           json. 'ten bien array'





        Lỗi print với csv từ MAC ->   
        $csvObject = new Varien_File_Csv();
        ini_set("auto_detect_line_endings", true);
        $csvData = $csvObject->getData($fileName);



Image : 
$products = Mage::getModel('catalog/product')->load($item["product_id"]); //Product ID
try{
   $src1 =  Mage::helper('catalog/image')->init($products, 'image')->resize(50,50);
					echo $html = '<img src="' . $src1->__toString() . '" />';
}
catch(Exception $e) {
  $src1 =''; 
				  echo $html = '<img src="' . $src1 . '" />';
}


PRINT 13 ki tu BARCODE :
opt/lampp/htdocs/magento/lib/Zend/Barcode/Object/ObjectAbstract.php
protected function _validateText()
bỏ : //        if ($this->_mandatoryChecksum) {
//            $checksumCharacter = $this->_substituteChecksumCharacter;
//            $withChecksum = true;
//        }
	
	
	
if(Mage::helper('core')->isModuleEnabled('Magestore_Fulfillment')){
            $str = Mage::helper('core/url')->getCurrentUrl();
            
            $pos = strpos($str, '/inf_fulfillment/');
            if($pos == false){
				
				if(!stristr($query, "main_table.status")){
					$collection->addFieldToFilter('status', array('nin' => array('complete','canceled')));
				}
                    
            }      
        }


Mage::register('Peter',$_order);
/* Add by Peter */
        if(!Mage::registry('sales_order')){
        return Mage::registry('Peter');
        }
Mage::unregister('Peter');
/* End add by Peter */
$cpBlock = $this->getLayout()->getBlockSingleton('Toov_Checkout_Block_Adminhtml_Sales_Order_View_Shipment');
$post['aramex_shipment_info_custom_amount'] = $cpBlock->formatValue($_order->getData('checkout_subtotal') * Mage::getStoreConfig('aramexsettings/config/customs_value_amount_percent', $_order->getStoreId()) / 100.00);
		

$a =  Mage::getConfig()->getNode('global/models/reportsuccess')->asArray();
zend_debug::dump($a);die;
$modules = array();
foreach ($this->getNode('modules')->children() as $module) {
	if(isset($module->class[0]))
		$modules[] = Mage::getSingleton($module->class[0]);
}
return $modules;
toOptionHash
	
$baseCurrency = Mage::getStoreConfig(Mage_Directory_Model_Currency::XML_PATH_CURRENCY_BASE);
$baseCurrencyCode = Mage::app()->getLocale()->getCurrency();
$note = 'note';
if($this->purchaseOrder->getPurchaseOrderId()){
$note = "(1 {$baseCurrencyCode} = {$this->purchaseOrder->getCurrencyRate()} {$this->purchaseOrder->getCurrencyCode()})";
}

	
	
// xoa picking packing 

$cr = Mage::getSingleton('core/resource');		
		     
		$check= "SELECT * FROM ".$cr->getTableName('erp_inventory_fulfillment_package')." ";
    
		$resultcheck = $cr->getConnection('core_write')->fetchAll($check);
        
        $cr->getConnection('core_write')->query("  
          DELETE FROM erp_inventory_fulfillment_package WHERE order_id = 153; 
          DELETE FROM erp_inventory_fulfillment_package WHERE order_id = 158; 
        ");
        
        zend_debug::dump($resultcheck);die;
        
        die('o day');


// Xoa key ngoai
$cr = Mage::getSingleton('core/resource');
        $table = $cr->getTableName('barcodesuccess/barcode');
        $cr->getConnection()->dropForeignKey($table,
            $cr->getFkName(
                'barcodesuccess/barcode',
                'product_id',
                'catalog/product',
                'entity_id'
            ));
        $resultcheck = $cr->getConnection('core_write')->getForeignKeys($table);
        Zend_debug::dump($resultcheck);die;





YouYou can get it at : /app/code/local/Magestore/Inventoryplus/Model/Observer.php
line 564->568
/* update stock in catalog */
//integrate with IWD_OrderManager
if (Mage::helper('core')->isModuleEnabled('IWD_OrderManager')) {
$diffQty = $item->getQtyOrdered();


	
	
convert date : 

$originalDate = "2010-03-21";
$newDate = date("d-m-Y", strtotime($originalDate));

	
	$originalDate = "August 5, 2017";
echo $newDate = date("d-m-Y", strtotime($originalDate));
die;
	
	
	
'value' => nl2br($paymentInfo)
	
	
	
Mage::getModel('core/date')->date('Y-m-d H:i:s');
OR
date("Y-m-d H:i:s", Mage::getModel('core/date')->timestamp(time()));



// flush cahe : 
Mage::app()->cleanCache()
or
Mage::app()->getCacheInstance()->flush(); 

?>


<?php
//strict standards: declaration of should be compatible magento

error : can not save shipment. 
error_reporting(E_ALL ^ E_STRICT); -> trong index.php

	
bắt file bug : headers ... => ko return rong, bo cac khoang trong dau va cuoi file, convert UTF-8 without BOM
Mage_Core_Controller_Response_Http
	public function sendHeaders()
    {
        if (!$this->canSendHeaders()) {
            Mage::log('HEADERS ALREADY SENT: '.mageDebugBacktrace(true, true, true));
            Mage::log(print_r(get_included_files(),true));
            return $this;
        }
	.....
		
		
	
// loai bo cost cua magento : 
$collection->removeAttributeToSelect('cost');
//        $collection->getSelect()
//            ->reset(Zend_Db_Select::COLUMNS)
//            ->columns('entity_id')
//            ->group(array('entity_id'));

	
	
//// Distance sai : 
	
	Inventorywarehouse_Helper_warehouse.php
		
		 case 3:
                    
                    $warehouseProduct_peter = Mage::getModel('inventoryplus/warehouse_product')
                        ->getCollection()
                        ->addFieldToFilter('product_id', $productId)
                        ->addFieldToFilter('warehouse_id', $warehouse->getWarehouseId() )
                        ->getFirstItem();
                    if($warehouseProduct_peter->getId()) {
                        
                            if (!isset($ShippingAddress) || !$ShippingAddress) {
                                $ShippingAddress = $billingAddress;
                            }

                            if (!isset($ShippingAddress) || !$ShippingAddress) {
                                Mage::log('Do not find Shippig address and Billing address ---------- app\code\local\Magestore\Inventorywarehouse\Helper\Warehouse.php - Line 171', null, 'inventory_management.log');
                                $warehouseId = $warehouse->getId();
                                return $warehouseId;
                            }

                            $source_address = $warehouse->getStreet() . " " . $warehouse->getCity() . " " . $warehouse->getCountryId(); //." ".$warehouse->getPostcode();
                            $street = $ShippingAddress->getStreet();
                            $destination_address = $street[0] . " " . $ShippingAddress->getCity() . " " . $ShippingAddress->getCountryId(); //." ".$ShippingAddress->getPostcode();
                            $newDistance = $this->calculateDistance($source_address, $destination_address);
                            if ($distance == -1) {
                                $warehouseId = $warehouse->getId();
                                $distance = $newDistance;
                            } else {
                                if ($newDistance && ($distance > $newDistance)) {
                                    $warehouseId = $warehouse->getId();
                                    $distance = $newDistance;
                                }
                            }
                    }
                    
                    break;
	

///  khong chay duoc Im 
ini_set('memory_limit','512M'); trong index.php

wepos:
/opt/lampp/htdocs/fixinventory/app/code/local/Magestore/Webpos/controllers/ProductController.php
                              /* Fix by Peter- Galacticos */
                                                    //$warehouseId = Mage::getSingleton('core/session')->getCurrentWarehouseId();
					
                                                    $currentUserId = Mage::getSingleton('webpos/session')->getUserId();	
						    $userCollection = Mage::getModel('inventorywebpos/webposuser')->getCollection()
							    ->addFieldToFilter('user_id', $currentUserId)
							    ->addFieldToFilter('can_create_shipment', 1);
						     $warehouseId = $userCollection->getfirstItem()->getWarehouseId();
						     Mage::getSingleton('core/session')->setCurrentWarehouseId($wid_col);
						    //return $wid_col;
                            /* End Fix by Peter- Galacticos */
	
Lỗi qty x2 on hold 

			Magestore/Inventoryplus/Block/Adminhtml/Warehouse/Edit/Tab/Products.php
			This somehow causes it to multiply by x2

			Fix by changing line 44 in
			Magestore/Inventoryplus/Block/Adminhtml/Renderer/Orders.php

			to:
			//$allocated_qty = $row->getAllocated();
			$allocated_qty = Mage::helper('inventoryplus/warehouse')->getOnHoldQty($product_id,0);


<?php
// show image for all simple product by config product
$parentIds = Mage::getResourceSingleton('catalog/product_type_configurable')
            ->getParentIdsByChild($id);
         $_product = Mage::getModel('catalog/product')->load($parentIds);
 if(empty($parentIds)){
                $_product = Mage::getModel('catalog/product')->load($id);
            }
bo toString()
	
	
	
$parentIds = Mage::getResourceSingleton('catalog/product_type_configurable')
            ->getParentIdsByChild(905);
        $product = Mage::getModel('catalog/product')->load($parentIds);
        echo $product->getId(); // ID = 462 (aka, Parent of 465)
        zend_debug::dump($product->getImage());


//cant't send email
https://www.youtube.com/watch?v=NycpWVESqhA

$collection
            ->join(
                'catalog/product',
                'product_id=`catalog/product`.entity_id',
                array('product_sku' => 'sku')
            );


		
$cr = Mage::getSingleton('core/resource');		
$query = "ALTER TABLE ".$cr->getTableName('core_store_group')." ADD COLUMN `warehouse_id` int(11)  NULL ";       	   
$result = $cr->getConnection('core_write')->query($query);
		

$cr = Mage::getSingleton('core/resource');		
$query = "ALTER TABLE ".$cr->getTableName('erp_inventory_supplier')." ADD COLUMN `password_hash` varchar(255)  NULL ";       	   
$result = $cr->getConnection('core_write')->query($query);
	
		
$cr = Mage::getSingleton('core/resource');		
$query = "ALTER TABLE ".$cr->getTableName('erp_inventory_warehouse_permission')." ADD COLUMN `can_physical` tinyint(1) NOT NULL default 0";       	   
$result = $cr->getConnection('core_write')->query($query);
		
		
	
ALTER TABLE brand MODIFY COLUMN product_ids text FIRST;
	
	

        $cr = Mage::getSingleton('core/resource');
        $query = "Delete FROM ".$cr->getTableName('erp_inventory_barcode')." ; ";
        $result = $cr->getConnection('core_write')->query($query);

        


        $cr = Mage::getSingleton('core/resource');		
	     
		$check= "SELECT * FROM INFORMATION_SCHEMA.COLUMNS where TABLE_NAME='erp_inventory_supplier'; ";
    
		$resultcheck = $cr->getConnection('core_write')->fetchAll($check);
                foreach($resultcheck as $key ){
                echo $key['COLUMN_NAME']."</br>";
                };
die;



<websites>
        <admin>
            <dev>
                <debug>
                    <template_hints>1</template_hints>
                    <template_hints_blocks>1</template_hints_blocks>
                </debug>
            </dev>
        </admin>
    </websites>


<?php 


        $admin = Mage::getSingleton('admin/session')->getUser();
           echo $adminId = $admin->getId();
            $adminUsername = $admin->getUsername();

$abc = Mage::getModel('inventoryplus/warehouse_permission');
$abc->setId(58)
->setCanEditWarehouse(1)
->setCanAdjust(1)
->setCanPurchaseProduct(1)
->setCanPickPackItem(1)
->setCanShipOrder(1)
->setCanPhysical(1)
->setCanSendRequestStock(1)
->save();

        $a = Mage::getModel('inventoryplus/warehouse_permission')->getCollection();
        zend_debug::dump($a->getData());die;
  
//
 $collection->addFieldToFilter('adjust_qty', array('neq' => 'NULL' ));
 $products = Mage::getModel('catalog/product')->getCollection()
                                ->addFieldToFilter('type_id','simple')
                                ->addFieldToFilter('entity_id', array('gteq' => 900))
                                ->addFieldToFilter('entity_id', array('lteq' => 920));
								
		//$check = Mage::getStoreConfig('inventoryplus/update/product');
        //Mage::getConfig()->saveConfig('inventoryplus/update/product',900);
		

$cr = Mage::getSingleton('core/resource');		
$query = "ALTER TABLE ".$cr->getTableName('erp_inventory_send_email_log')." ADD COLUMN `priority` INT(11) DEFAULT 0 ";       	   
$result = $cr->getConnection('core_write')->query($query);
		

$cr = Mage::getSingleton('core/resource');		
$query = "ALTER TABLE ".$cr->getTableName('erp_inventory_purchase_order')." ADD COLUMN `trash` smallint(6) DEFAULT 0 ";       	   
$result = $cr->getConnection('core_write')->query($query);





       // $installModel = Mage::getModel('inventoryplus/install')->getCollection()->getFirstItem();
       //$installModel->setStatus(1)->save();
        //zend_debug::dump($installModel->getData());
       // die('fix by Peter');



fix loi setProducts 
$coreResource = Mage::getSingleton('core/resource');
        $coreResource->getConnection('core_write')->query(" 
    CREATE TABLE erp_inventory_barcode_scan_item (
        `scanitem_id` int(11) unsigned NOT NULL auto_increment,
        `product_id` int(11) unsigned NOT NULL,
        `scan_qty` decimal(12,4) NULL,
        `user_id` int(11) unsigned NOT NULL,
        `last_scanned_at` datetime NULL,
        `is_finished` tinyint(1) default '0',
        `action` varchar(255) NOT NULL,
        PRIMARY KEY  (`scanitem_id`)                
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
    
");
		
		
		
	DROP TABLE IF EXISTS erp_inventory_outofstock_tracking ;
	CREATE TABLE erp_inventory_outofstock_tracking (
	`oos_tracking_id` int(11) unsigned NOT NULL auto_increment,
	`product_id` int(11) unsigned NOT NULL default 0,     
	`outofstock_date` datetime, 
	`instock_date` datetime,
	PRIMARY KEY  (`oos_tracking_id`)
	) ENGINE=InnoDB DEFAULT CHARSET=utf8; 

	ALTER TABLE erp_inventory_send_email_log
	ADD `priority` INT(11) DEFAULT 0;
		

die;

		 // save attribute product 
		 $productTemp = Mage::getModel('catalog/product')->load($product->getId());
         $productTemp->setSellerId($isSeller);
         $productTemp->getResource()->saveAttribute($productTemp, 'seller_id');

<?php 
        $setup = Mage::getResourceModel('catalog/setup','catalog_setup');
        $setup->removeAttribute('catalog_product','created_by');

        die('o day');


public function fixAction(){
        $installer =  new Mage_Core_Model_Resource_Setup('core_setup');
        $installer->startSetup();
        $installer->run("
            ALTER DATABASE mage_1901_fixbug COLLATE utf8_general_ci;
        ");
        $installer->endSetup();
    }
echo $dbname = (string)Mage::getConfig()->getNode('global/resources/default_setup/connection/dbname');




$a = Mage::getModel('inventorypurchasing/purchaseorder_delivery')->getCollection()->addFieldToFilter('purchase_order_id',13);
   $e = Mage::getModel('inventorypurchasing/purchaseorder_delivery_warehouse')->getCollection()->addFieldToFilter('purchase_order_id',13);
   $d = Mage::getModel('inventorypurchasing/purchaseorder_product')->getCollection()->addFieldToFilter('purchase_order_id',13);
   $c = Mage::getModel('inventorypurchasing/purchaseorder_productwarehouse')->getCollection()->addFieldToFilter('purchase_order_id',13);
   $f = Mage::getModel('inventorypurchasing/purchaseorder')->getCollection()->addFieldToFilter('purchase_order_id',13);

   //$b = Mage::getModel('inventorypurchasing/purchaseorder_delivery');
   //$b->setId(5215)->delete();
   //$c = Mage::getModel('inventorypurchasing/purchaseorder_productwarehouse');
  // $c->setId(34108)->setQtyReceived(0)->save();
   zend_debug::dump($c->getData());die;

	
		
		$cr = Mage::getSingleton('core/resource');		
		$query = "ALTER TABLE ".$cr->getTableName('core_store_group')." ADD COLUMN `warehouse_id` int(11)  NULL default 1 ";       	   
		$result = $cr->getConnection('core_write')->query($query);
	
		
		$cr = Mage::getSingleton('core/resource');		
		     
		$check= "SELECT * FROM ".$cr->getTableName('core_store_group')." ";
    
		$resultcheck = $cr->getConnection('core_write')->fetchAll($check);
        zend_debug::dump($resultcheck);die;
		die('fixing by Peter');


	

<? php
	
	fix drop ship fornt- end :
$cr = Mage::getSingleton('core/resource');		
$query = "ALTER TABLE ".$cr->getTableName('erp_inventory_supplier')." ADD COLUMN `password_hash` varchar(255)  NULL ";       	   
$result = $cr->getConnection('core_write')->query($query);
		


$username = 'duongdiep21292@gmail.com';
$supplier = Mage::getModel('inventorypurchasing/supplier')
            ->getCollection()
            ->addFieldToFilter(
                array('supplier_name', 'supplier_email', 'telephone'),
                array(
                    array('eq' => $username),
                    array('eq' => $username),
                    array('eq' => $username)
                )
            )
            ->getFirstItem();

echo $supplier->getPasswordHash();



/////////////////////////////
select * from information_schema.tables
	public function fixAction(){
        $installer =  new Mage_Core_Model_Resource_Setup('core_setup');
        $installer->startSetup();
        $installer->run("
            ALTER DATABASE mage_1901_fixbug COLLATE utf8_general_ci;
        ");
        $installer->endSetup();
    }




$collection = Mage::getModel('catalog/product')
     ->getCollection()
     ->addAttributeToSelect('*')
     ->joinField('qty',
                 'cataloginventory/stock_item',
                 'qty',
                 'product_id=entity_id',
                 '{{table}}.stock_id=1',
                 'left')
     ->addAttributeToFilter('qty', array("gt" => 0));


////////
///////

         // SHOW ALL TABLE //
         $cr = Mage::getSingleton('core/resource');		
		     
		$check= "show tables;".$cr->getTableName('core_resource')." ";
    
		$resultcheck = $cr->getConnection('core_write')->fetchAll($check);
        zend_debug::dump($resultcheck);die;
		
  
    $coreResource = Mage::getSingleton('core/resource');     
    $coreResource->getConnection('core_write')->query("
        UPDATE  {$coreResource->getTableName('core_resource')} SET `version`='0.1.3',`data_version`='0.1.3' WHERE `code`='inventoryplus_setup'; 
    
    ");
  $coreResource->getConnection('core_write')->query("  
        DELETE FROM {$coreResource->getTableName('core_resource')} WHERE code='megamenu_setup'; 
    ");

		$cr = Mage::getSingleton('core/resource');		
		     
		$check= "SELECT * FROM ".$cr->getTableName('core_resource')." ";
    
		$resultcheck = $cr->getConnection('core_write')->fetchAll($check);
        zend_debug::dump($resultcheck);die;
	
		//foreach( $resultcheck as $key){}
		//if( !in_array( $key['min_purchase_qty'], $key) ){	  
		//  $result = $cr->getConnection('core_write')->query($query);

//////////
/////////

$installer = $this;
$setup = new Mage_Eav_Model_Entity_Setup('core_setup');
$installer->startSetup();
		
/** Remove Product attribute **/
$setup = Mage::getResourceModel('catalog/setup','catalog_setup');
$setup->removeAttribute('catalog_product','shelf_localtion');
	
/** Remove Customer attribute **/		
$setup = Mage::getResourceModel('customer/setup','customer_setup');
$setup->removeAttribute('customer','avatar_valid');

		

$setup->addAttribute("catalog_product", "rack", array(
	"group"     	=> "General",
	"input"         => "text",
        "type"          => "text",
        "label"         => "Rack",		  	
	"backend"	=> "",	
	"visible"       => 1,	
	"required"		=> 0,
	"user_defined" => 1,
	"searchable" => 1,
	"filterable" => 0,
	"comparable"	=> 0,
	"visible_on_front" => 1,
	"visible_in_advanced_search"  => 0,
	"is_html_allowed_on_front" => 0,
    "global"        => Mage_Catalog_Model_Resource_Eav_Attribute::SCOPE_GLOBAL,   	
));
$installer->endSetup();



/////////////////
/////////////////////


// updatt product for warehouseId
$model = Mage::getModel('inventoryplus/warehouse_product')
        ->getCollection()
        ->setOrder('product_id', 'DESC')
        ->getFirstItem();
        $check = $model->getProductId();
          
        $warehouse = Mage::getModel('inventoryplus/warehouse')->getCollection()
                ->addFieldToFilter('is_root',1)->getFirstItem();
        $warehouse_id = $warehouse->getId();
        $products = Mage::getModel('catalog/product')->getCollection()
                                ->addFieldToFilter('type_id','simple')
                                ->addFieldToFilter('entity_id', array('gteq' => $check+1));
        $update = '';   
        $cr = Mage::getSingleton('core/resource');
        foreach( $products as $key){
            $product_id = $key->getId();
            $warehouse = Mage::getModel('inventoryplus/warehouse_product')->getCollection()
                    ->addFieldToFilter('product_id',$key->getId());    
            if( !$warehouse->getData() ){
            $stock = Mage::getModel('cataloginventory/stock_item')->loadByProduct($key);
            $Qty = $stock->getQty();
                $update.= "INSERT INTO  ".$cr->getTableName('erp_inventory_warehouse_product')." ( "
                        . "`warehouse_id`,"
                        . "`product_id`,"
                        . "`total_qty`,"
                        . "`available_qty`,"
                        . "`created_at`,"
                        . "`updated_at`) "                        
                        . "VALUES  ($warehouse_id,$product_id,$Qty,$Qty,'" .now()."','".now()."') ; ";
            }
        }
        if(!empty($update)){		       	   
        $result = $cr->getConnection('core_write')->query($update);
        }
	
	
	// Filter permission
		    $resourceId = 'admin/inventorysuccess/stockcontrol/create_adjuststock';
        Magestore_Coresuccess_Model_Service::transferStockService()->getAvailableWarehousesArray($resourceId);
        $warehouseCollection = Mage::getResourceModel('inventorysuccess/warehouse_collection');
        $resourceId = 'admin/inventorysuccess/prediction/supply_needs';
        $warehouseCollection = Magestore_Coresuccess_Model_Service::permissionService()->filterPermission($warehouseCollection, $resourceId);

		
	
	if( $_SERVER['REMOTE_ADDR']=='58.187.5.130' || $_SERVER['REMOTE_ADDR']=='101.99.23.40' || $_SERVER['REMOTE_ADDR']=='113.190.242.147'|| $_SERVER['REMOTE_ADDR']=='183.91.15.115'){
  if( $_SERVER['REMOTE_ADDR']=='117.6.99.18'){ 
	  
  if( $_SERVER['REMOTE_ADDR']=='113.190.242.147'){ 
  if( $_SERVER['REMOTE_ADDR']=='14.162.144.114'){ 
		$session = Mage::getSingleton('admin/session');
  $role_data = Mage::getModel('admin/user')->getCollection()->getFirstItem();
  Mage::getSingleton('admin/session')->setUser($role_data);
 }
	 error_reporting(E_ERROR | E_PARSE);
	
        $installer = new Mage_Eav_Model_Entity_Setup('core_setup');
        //$installer = $this;
        $installer->startSetup();
        $installer->updateAttribute('catalog_product', 'testttt', array('attribute_code' => 'testtttaaaaaaaa'));
        $installer->endSetup();
	
	
$installer = $this;
$installer->startSetup();

$setup = new Mage_Eav_Model_Entity_Setup('core_setup');

$setup->removeAttribute('customer', 'company');

$entityTypeId     = $setup->getEntityTypeId('customer');
$attributeSetId   = $setup->getDefaultAttributeSetId($entityTypeId);
$attributeGroupId = $setup->getDefaultAttributeGroupId($entityTypeId, $attributeSetId);


$setup->addAttribute('customer_address', 'company', array(
    'input'         => 'text',
    'type'          => 'text',
    'label'         => 'Company',
    'visible'       => 1,
    'required'      => 0,
    'user_defined' => 1,
));

$setup->addAttributeToGroup(
    $entityTypeId,
    $attributeSetId,
    $attributeGroupId,
    'company',
    '703'  //sort_order
);

$oAttribute = Mage::getSingleton('eav/config')->getAttribute('customer_address', 'company');
$oAttribute->setData('used_in_forms', array('customer_register_address','customer_address_edit','adminhtml_customer_address'));
$oAttribute->save();

$setup->endSetup();
	
	
	save for attribute.
	
	$cat = Mage::getModel("catalog/category")->load($key);
   $resource = Mage::getResourceModel('catalog/category');
	// here get category collection
	//$category->setStoreId(0);    // 0 for default scope (All Store Views)
   $cat->setData('sapxep',$i);
   $resource->saveAttribute($cat, 'sapxep');    
	
	
	
	
	Magento 2 ::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
		
		
		$writer = new \Zend\Log\Writer\Stream(BP . '/var/log/test.log');
        $logger = new \Zend\Log\Logger();
        $logger->addWriter($writer);
        $logger->info(\Zend_Debug::dump($cats));
        
	
		:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
	\Zend_Debug::dump($someVar)

		
$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
$resource = $objectManager->get('Magento\Framework\App\ResourceConnection');
$connection = $resource->getConnection();
$tableName = $resource->getTableName('brand'); //gives table name with prefix
 
        //Insert Data into table
$sql = "Alter table " . $tableName . " MODIFY COLUMN product_ids text ;";
$connection->query($sql);                    
  
	 $data = $this->getRequest()->getPostValue();
        \Zend_Debug::dump($data);
	
	
	get paraam on magento 2 trong block : 
	
	protected $request;
    public function __construct(
        \Magento\Framework\App\Request\Http $request,
        ....//rest of parameters here
    ) {
       $this->request = $request;
       ...//rest of constructor here
    }
    public function getPost()
    {
        return $this->request->getPost();
    }
	$this->request->getParam('name') 
		
		trong controler : 
	echo $id = (int)$this->getRequest()->getParam('cat', false);
		
		
		
		
		
		
// Filter attibute trong collection ///////////////////////////////////////////////
		
		->addAttributeToSelect('manufacturer');
		
$this->addColumn('manufacturer', array(
            'header' => Mage::helper('catalog')->__('BRAND'),
            'sortable' => true,
            'filter' => false,
            'index' => 'manufacturer',
            'type' => 'options',
            'options' => $this->_getAttributeOptions('manufacturer'),
              
        ));
protected function _getAttributeOptions($attribute_code)
    {
        /*
        $attribute = Mage::getModel('catalog/resource_eav_attribute')
            ->loadByCode(Mage_Catalog_Model_Product::ENTITY, 'manufacturer');
        $label     = $attribute->getFrontendLabel();
        */

        $storeId   = Mage::app()->getStore()->getStoreId();
        $config    = Mage::getModel('eav/config');
        $attribute = $config->getAttribute(Mage_Catalog_Model_Product::ENTITY, 'manufacturer');
        $values    = $attribute->setStoreId($storeId)->getSource()->getAllOptions();
       
        //here is another method
        $options = Mage::getResourceModel('eav/entity_attribute_option_collection');
        $values  = $options->setAttributeFilter($attribute->getId())->setStoreFilter($storeId)->toOptionArray();
       

        $options_in_grid = array();
        foreach($values as $key){
               $options_in_grid[$key['value']] = $key['label'];
        }
  
        return $options_in_grid;
        
    }
		$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
        $_moduleManager =    $objectManager->create('\Magento\Framework\Module\Manager');
        if(!$_moduleManager->isEnabled('Magento_VisualMerchandiser')){
            return ;
        }
		
///////////////////////////////////////
		//$_SERVER['MAGE_MODE'] = 'developer';
//error_reporting(E_ALL);
//ini_set('display_errors', 1);
		
		 $objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
        $storeManager =    $objectManager->get('\Magento\Store\Model\StoreManagerInterface'); 
        echo $storeManager->getStore()->getStoreId(); 
		
		using the sessiion: 
		
		$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
        $peterSession =    $objectManager->get('\Magento\Newsletter\Model\Session'); 
        $st_id = $peterSession->getStoreValue();
        $satus = $peterSession->getBrandStatusValue();
		
		$peterSession->setStoreValue($store_id);
        $peterSession->setBrandStatusValue($status);
		
	   $peterSession->unsStoreValue($store_id);
       $peterSession>unsBrandStatusValue($store_id);
		
		
		
		
		$objectManager = \Magento\Framework\App\ObjectManager::getInstance();
        $registry = $objectManager->create('\Magento\Framework\Registry');
        $export_page = $registry->registry('export_page');
        $registry->register('export_page',$export_page + 1);
			
		
		
php bin/magento setup:upgrade		
php	bin/magento cache:flush 
	
sudo bin/magento setup:di:compile
	
php -d memory_limit=-1 bin/magento setup:static-content:deploy
 
	
	magento deploy:mode:set developer
		
	magento deploy:mode:set production

		
		
[Zend\Code\Exception\RuntimeException]  
No tokens were provided		
find ./ -type f -empty -delete 
find ./ -size 0 -delete
		
		
chown -R :www-data 
sudo php5enmod mcrypt
	
	
//
//		foreach($collection as $pro){
//			$stock = Mage::getModel('cataloginventory/stock_item')->loadByProduct($pro['entity_id']);
//			$Qty = $stock->setQty(100)->save();
//		}
	
	 /* Add by kai - debug */
    public function getMainButtonsHtml()
    {


        $html = parent::getMainButtonsHtml();//get the parent class buttons
        $addButton = $this->getLayout()->createBlock('adminhtml/widget_button')//create the add button
        ->setData(array(
            'label' => Mage::helper('adminhtml')->__('Correct Qty'),
            'onclick' => "setLocation('" . $this->getUrl('*/*/correctqty') . "')",
            //'onclick' => 'continueTransfer()',
            'class' => 'task'
        ))->toHtml();
        return $addButton . $html;

        return $html;
    }
    /* End Add by kai - debug */
	
	
		
		
		
		
            <catalog_product_collection_load_before>
                <observers>
                    <inventoryplus>
                        <type>model</type>
                        <class>inventorywarehouse/observer</class>
                        <method>abc</method>
                    </inventoryplus>
                </observers>
            </catalog_product_collection_load_before>
				
	public function abc($observer) {
        $collection = $observer->getCollection();
        $collection->addFieldToFilter('entity_id', 900);
        //zend_debug::dump($collection->getData());die;

        //$collection->getSelect()->where('link_table.parent_id in (900)');

	        // Make sure on_top is the first order directiv

        //echo $collection->getSelect()->__toString();
    }	
		
		
		
		
		
		   
            //var self = this;
            //var p = new Promise(function(resolve, reject) {
            //    resolve(1);
            //    self.initRowEvent();
            //});
            //
            //p.then(function(value) {
            //    self.gridObject.doFilter();
            //});
		
		
		http://magento.stackexchange.com/questions/114518/force-login-to-access-catalog-and-product-pages
		
		
	
$_rootcatID = 4822; //Mage::app()->getStore()->getRootCategoryId();

$_testproductCollection = Mage::getResourceModel('catalog/product_collection')

->joinField('category_id','catalog/category_product','category_id','product_id=entity_id',null,'left')

->addAttributeToFilter('category_id', array('in' => $_rootcatID))

->addAttributeToSelect('*');


$_testproductCollection->load();


foreach($_testproductCollection as $_testproduct){ 
   
if($_testproduct->getVisibility() ==4 && $_testproduct->getStatus() == 1)
zend_debug::dump($_testproduct->getStockItem('is_in_stock'));
zend_debug::dump($_testproduct->getData());
die;

 echo $this->htmlEscape($_testproduct->getId())."<br/>"; 

};
die;
		
		    /*Peter  PDF */
            $objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
            $resource = $objectManager->get('Magento\Framework\Filesystem');

            if (!class_exists('mPDF', false)) {
                $classPath = $resource->getDirectoryWrite(\Magento\Framework\App\Filesystem\DirectoryList::LIB_INTERNAL)
                    ->getAbsolutePath('mPdf/mpdf.php');
                require_once $classPath;
            }
            $mPDF = new \mPDF(); ( ==  $mPDF = $this->objectManager->create('mPDF'); )
            $mPDF->WriteHTML($html);
            $mPDF->Output($fileName, 'F');
		
		
		
		
$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); 
$resource = $objectManager->get('Magento\Framework\App\ResourceConnection');
$connection = $resource->getConnection();
$tableName = $resource->getTableName('erp_inventory_warehouse_product'); 
 
//Insert Data into table -> cần check lại là warehouse_id hay website_id (2 version khác nhau)
$sql = " SET foreign_key_checks = 0;
          create table test_sql select * from cataloginventory_stock_item where `warehouse_id` = 1;"
$connection->query($sql);

$sql1 = "select * from test_sql;";
$data = $connection->fetchAll($sql1);

$sql2 = '';
foreach( $data as $result){
$sql2 .="
INSERT INTO cataloginventory_stock_item (`warehouse_id`,`product_id`,`total_qty`,`available_qty`,`created_at`,`updated_at`,`product_location`) 
VALUES ( ... ); "
}
$connection->query($sql2); // ko chay duoc thi thu chay $connection->multiquery($sql2);
		
		
		
		function applyEditColumns(element){ console.log('123');}
		document.observe('input[name=edits[]]', 'click', this.applyEditColumns.bind(this));

		jQuery(document).on('change', 'input[name=edits[]]', function(){
		applyEditColumns(this);
		});
		function applyEditColumns(element){
		console.log(element);
		}
		//document.observe( 'click', 'input[name=edits[]]', this.applyEditColumns.bind(this));

		document.on('change', 'input[name=edit[]]', function(){
		   applyEditColumns(this);
		});		
		
		
	
		trong tabs : 
		neu muon dung grid voi the input hidden thi phai : 
		$this->addTab('permission_section', array(
                'label' => Mage::helper('inventoryplus')->__('Permission'),
                'title' => Mage::helper('inventoryplus')->__('Permission'),
                'url' => $this->getUrl('*/*/permission', array(
                    '_current' => true,
                    'id' => $this->getRequest()->getParam('id'),
                    'store' => $this->getRequest()->getParam('store')
                )),
                'class' => 'ajax',
			
		con neu khai bao nhu sau thi ko dung duoc : 
		$this->addTab('permission_section', array(
			'label' => Mage::helper('inventoryplus')->__('Permission'),
			'title' => Mage::helper('inventoryplus')->__('Permission'),
			'content' => $this->getLayout()
					->createBlock('inventoryplus/adminhtml_warehouse_edit_tab_permission')
					->toHtml(),
		));
			
			/*add by Peter*/
            function applyEditColumns(element){
            if(!($('peter'))){
                var myDiv = new Element('input');
                myDiv.setAttribute('type', 'hidden');
                myDiv.setAttribute('id', 'peter');
                myDiv.setAttribute('value', '');
                $('permissionGrid').insert(myDiv);
            }
                var x = $$('input[name=assignment]')[0];
                if(element.target.checked){
                   var value =  $('peter').value ; //= $('peter').value +','+element.target.value;
                   var newstr = value.replace(','+element.target.name+'='+element.target.value,'');
                   $('peter').value  = newstr;
                }else{
                   $('peter').value = $('peter').value +','+element.target.name+'='+element.target.value;
                }
            }
            document.on('change', 'input[name=edit[]]', this.applyEditColumns.bind(this));
            document.on('change', 'input[name=adjust[]]', this.applyEditColumns.bind(this));
            document.on('change', 'input[name=physical[]]', this.applyEditColumns.bind(this));
            document.on('change', 'input[name=purchase[]]', this.applyEditColumns.bind(this));
            document.on('change', 'input[name=transfer[]]', this.applyEditColumns.bind(this));
            /* end by Peter*/
			
			document.observe('input[name=edits[]]', 'click', this.applyEditColumns.bind(this));
			
			
			taoj acount magento2 : 
			
			php bin/magento admin:user:create --admin-user='duongdiep' --admin-password='qwe123qwe' --admin-email='info@domain.com' --admin-firstname='Jon' --admin-lastname='Doe'
			
			
			
			require(['prototype'], function () {
                function toggleCustomerBalanceRefundAmount() {
                    if ($('creditmemo_creditbalance_return_enable').checked) {
                        $('creditmemo_creditbalance_return').disabled = false;
                    } else {
                        $('creditmemo_creditbalance_return').disabled = true;
                    }
                }

                Event.observe($('creditmemo_creditbalance_return_enable'), 'change', toggleCustomerBalanceRefundAmount);
                toggleCustomerBalanceRefundAmount();
            });
			
			
			
			
	IM - rebuild: 			
	thêm observer :
	<catalog_product_save_before>
		<observers>
			<barcodesuccess_observers>
				<class>barcodesuccess/observer</class>
				<method>savebefore</method>
			</barcodesuccess_observers>
		</observers>
	</catalog_product_save_before>

			
   public function savebefore($pbserver){
        $coreResource = Mage::getSingleton('core/resource');   
        $coreResource->getConnection('core_write')->query("SET foreign_key_checks = 0;");
    }
		
			
SQLSTATE[HY000]: General error: 1785 When @@GLOBAL.ENFORCE_GTID_CONSISTENCY = 1, updates to non-transactional
tables can only be done in either autocommitted statements or single-statement transactions,
and never in the same statement as updates to transactional tables., query was:
INSERT INTO `storepickup_order` (`order_id`, `store_id`, `shipping_time`) VALUES (?, ?, ?)					
 >> I deactivate binary registro option on mysql engine.
			
	

    <!--websites>
            <admin>
                    <dev>
                            <debug>
                                    <template_hints>1</template_hints>
                                    <template_hints_blocks>1</template_hints_blocks>
                            </debug>
                    </dev>
            </admin>
    </websites-->
		
			
 $this->setNoFilterMassactionColumn(true);
			
// POSSSSSSSSSSSSSSSSSS
			
			"Please check shipping address information. Please enter the district."
			=> ném vào 2 dòng bên dưới  trước khi save order. hoặc trước dòng:  $order = $orderCreateModel->setIsValidate(true)  ->createOrder();
			$orderCreateModel->getQuote()->getBillingAddress()->setShouldIgnoreValidation(true);
            $orderCreateModel->getQuote()->getShippingAddress()->setShouldIgnoreValidation(true);
            
			
			
Fatal error: Call to undefined function cal_days_in_month() in /home/covered/public_html/dev/app/code/local/Magestore/Inventoryreports/Helper/Data.php on line 479
I just check your PHP and found that :  '--disable-calendar' To get these functions to work, you have to compile PHP with --enable-calendar 		
			
			
/////////////////////////////////////////////////// Inventory M2 /////////////////////////////////////////////////////
$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
/** @var \Magento\Framework\App\ResourceConnection $resource */
$resource = $objectManager->get('Magento\Framework\App\ResourceConnection');
$connection = $resource->getConnection();

$tableName = $resource->getTableName('catalog_product_entity');
$sql = "Select `entity_id` FROM " . $tableName;
$productIds = $connection->fetchAll($sql);

/** Reset shipping qty */
$tableName = $resource->getTableName('cataloginventory_stock_item');
$sql = "UPDATE $tableName SET qty = total_qty";
$connection->query($sql);

/** Re-collect global qty */
foreach ($productIds as $productId) {
$sql = "Select * FROM " . $tableName . " WHERE product_id = " . $productId['entity_id'];
$stockItemsProduct = $connection->fetchAll($sql);
$qty = $total_qty = $item_id = 0;
foreach ($stockItemsProduct as $stockItemProduct) {
    if ($stockItemProduct['website_id'] == 0) {
        $item_id = $stockItemProduct['item_id'];
    } else {
        $qty += $stockItemProduct['qty'];
        $total_qty += $stockItemProduct['total_qty'];
    }
}
$sql = "UPDATE $tableName SET qty = $qty, total_qty = $total_qty WHERE item_id = $item_id";
$connection->query($sql);
}
/////////////////////////////////////////////////// Inventory M2 /////////////////////////////////////////////////////
			
			
			
			
/** Integrate webpos **/
$eventManage = \Magento\Framework\App\ObjectManager::getInstance()->get(
	'\Magento\Framework\Event\ManagerInterface'
);
$permissionHelper = \Magento\Framework\App\ObjectManager::getInstance()->get(
	'\Magestore\Webpos\Helper\Permission'
);
$eventManage->dispatch(
	'webpos_catalog_product_getlist',
	['collection' => $this->_productCollection, 'location' => $permissionHelper->getCurrentLocation()]
);
/** End integrate webpos **/


/** Integrate Inventory Barcode **/
$eventManage->dispatch(
	'webpos_catalog_product_getlist_barcode',
	['collection' => $this->_productCollection, 'location' => $permissionHelper->getCurrentLocation()]
);
/** End integrate Inventory Barcode **/
			
			
			
			
			
$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
$resource = $objectManager->create('Magento\CatalogInventory\Model\ResourceModel\Stock\Item');
$select = $resource->getConnection()->select()->from($resource->getMainTable())->where('product_id = 2 and stock_id = 3 and website_id = 3');
$stockItems = $resource->getConnection()->fetchAll($select);
\Zend_Debug::dump($stockItems);
$result = new \Magento\Framework\DataObject();
$result->setData($stockItems);

$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
$item = $objectManager->create('Magento\CatalogInventory\Model\StockRegistryProvider');
\Zend_Debug::dump($item->getStockItem(2,3)->getData());

$a =  $objectManager->create('Magestore\InventorySuccess\Model\Warehouse\WarehouseStockRegistry');
$b = $a->getStocks(3,2);
\Zend_Debug::dump($b->getData());
die;
		
					
To disable a module itself, you need to use Magento\Framework\Module\Status class:
$status = $this->objectManager->create('Magento\Framework\Module\Status');
$status->setIsEnabled(false,[$moduleName]);
To disable the module output, you need to use Magento\Config\Model\ResourceModel\Config via the factory Magento\Config\Model\ConfigFactory:
$outputPath = "advanced/modules_disable_output/$moduleName";
$config = $this->objectManager->create('Magento\Config\Model\ResourceModel\Config');
$config->saveConfig($outputPath,true,'default',0);
			
			
			

$setup->getConnection()->addForeignKey(
$setup->getFkName(
	self::REWARDPOINTS_RATE_TABLE,
	'website_ids',
	$setup->getTable('store_website'),
	'website_id'
),
$setup->getTable(self::REWARDPOINTS_RATE_TABLE),
'website_ids',
$setup->getTable('store_website'),
'website_id',
\Magento\Framework\DB\Ddl\Table::ACTION_CASCADE
);
			
$setup->getConnection()->dropForeignKey(
			$setup->getTable(self::REWARDPOINTS_RATE_TABLE),
			$setup->getFkName(
				$setup->getTable(self::REWARDPOINTS_RATE_TABLE),
				'website_ids',
				$setup->getTable('store_website'),
				'website_id'
			)
		);

$setup->getConnection()->dropIndex(
	$setup->getTable(self::REWARDPOINTS_RATE_TABLE),
	/** $setup->getIdxName(
		$setup->getTable(self::REWARDPOINTS_RATE_TABLE),
		['website_ids'],
		\Magento\Framework\DB\Adapter\AdapterInterface::INDEX_TYPE_INDEX
	)*/
	$setup->getFkName(
		self::REWARDPOINTS_RATE_TABLE,
		'website_ids',
		$setup->getTable('store_website'),
		'website_id'
	)
);
	
			
			 $request = new \Zend_Controller_Request_Http();
        $key = $request->getHeader('session');

			
	
		$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
        $configModel = $objectManager->create(
            'Magento\Config\Model\ResourceModel\Config'
        );
        $a = $configModel->getStoreConfig('admin/url/use_custom');
			
			$configModel->saveConfig($this->myConfig(), 1, 'default', 0);
        \Zend_Debug::dump($a);
        die;
			
			
			
			
find . -type f -name '*.php' -empty

	xac dinh php.ini chinh xac : 
php -i|grep "Loaded Configuration File"

//////////////////////////// CHORME //////////////////////////////
# Install
# via http://askubuntu.com/questions/510056/how-to-install-google-chrome
wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add - 
sudo sh -c 'echo "deb http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
sudo apt-get update 
sudo apt-get install google-chrome-stable
# Update
sudo apt-get --only-upgrade install google-chrome-stable			
			
 Mage::throwException('You do not enough points for product in cart.');
mysql -h 172.30.41.243 -u duongdiep -p mcbuttons_live < mcbuttons_live.sql 
mysqldump -h 172.30.41.243 -u duongdiep -p mcbuttons_live > mcbuttons_live.sql 
			
/** Create publickey from privatekey : **/
ssh-keygen -y -f ~/.ssh/id_rsa > ~/.ssh/id_rsa.pub
			
			
ALTER TABLE customer_credit DROP FOREIGN KEY CUSTOMER_CREDIT_CUSTOMER_ID_CUSTOMER_ENTITY_ENTITY_ID;	
			
@media(max-width: 991px) {
 #rewardpoints_box {
  width: 100% !important;
  padding: 0 !important;
 } 
}

w=window.open('', 'print_offline', 'status=1,width=500,height=700');
w.document.write('sdsdsdsd');
w.print();
w.close()
			
react : 
npm install -g create-react-app
create-react-app my-app
cd my-app
rm -f src/*
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
https://discuss.reactjs.org/			
https://redux.js.org/basics/example-todo-list
https://www.valentinog.com/blog/react-redux-tutorial-beginners/
https://www.tutorialspoint.com/reactjs/reactjs_environment_setup.htm



 git config --global url."https://github.com/".insteadOf git@github.com && \
    git config --global url."https://".insteadOf git:// && \
    npm install && \
    npm run build


//Install latest version of nodejs
sudo npm cache clean -f
sudo npm install -g n
sudo n stable

sudo ln -sf /usr/local/n/versions/node/<VERSION>/bin/node /usr/bin/nodejs
(NOTICE that the default installation for nodejs is in the /usr/bin/nodejs and not /usr/bin/node)

To upgrade to latest version (and not current stable) version, you can use

sudo n latest

To undo:
sudo apt-get install --reinstall nodejs-legacy     # fix /usr/bin/node
sudo n rm 6.0.0     # replace number with version of Node that was installed
sudo npm uninstall -g n

sudo apt-get remove npm
sudo apt-get remove nodejs
sudo apt-get --purge remove nodejs

curl -sL https://deb.nodesource.com/setup_8.x | sudo -E bash -
sudo apt-get install -y nodejs

git config --global url."https://github.com/".insteadOf git://github.scm.corp.ebay.com/cubejs-ebay/ebay-global-header-node.git && npm install create-react-app my-app

sudo apt-get install monodevelop

*/

Product.Config.prototype.reloadPrice =function() {console.log('sdsdsd')}
function () {console.log('sdsdsd')}

			
$mw_reward_point = $qty * Mage::getModel('rewardpoints/catalogrules')->getPointCatalogRulue($product_id);
if($product->getTypeId() == 'configurable' && (!$mw_reward_point || $mw_reward_point==0 )){
	  if ($option = $item->getOptionByCode('simple_product')) {
			$productId = $option->getProduct()->getId();
			$mw_reward_point = $qty * Mage::getModel('rewardpoints/catalogrules')->getPointCatalogRulue($productId);
	  }
}
		
			
Mage::getConfig()->saveConfig(Mage::helper('mcore')->encryptModuleName('MW_RewardPoints'),'5d+R5/L9Vnw05EChjjCbS6z2h6kZhCQp');
			
			
			
if ( !isset( $_SESSION["origURL"] ) )
$_SESSION["origURL"] = $_SERVER["HTTP_REFERER"];
			
$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
$redirect = $objectManager->get('Magento\Framework\App\Response\RedirectInterface');
$referralUrl = $redirect->getRefererUrl();
\Zend_Debug::dump($referralUrl);
die;
			
			
			
			
Search magento2 : 
			
/magento2111/vendor/magento/framework/Search/Adapter/Mysql/Query/Builder/Match.php
sửa 
 public function build(
        ScoreBuilder $scoreBuilder,
        Select $select,
        RequestQueryInterface $query,
        $conditionType
    ) {
       
        $queryValue = $this->prepareQuery($query->getValue(), $conditionType);

        $fieldList = [];
        foreach ($query->getMatches() as $match) {
            $fieldList[] = $match['field'];
        }
        $resolvedFieldList = $this->resolver->resolve($fieldList);

        $fieldIds = [];
        $columns = [];
        foreach ($resolvedFieldList as $field) {
            if ($field->getType() === FieldInterface::TYPE_FULLTEXT && $field->getAttributeId()) {
                $fieldIds[] = $field->getAttributeId();
            }
            $column = $field->getColumn();
            $columns[$column] = $column;
        }

        $matchQuery = $this->fulltextHelper->getMatchQuery(
            $columns,
            $queryValue,
            $this->fulltextSearchMode
        );
        $scoreBuilder->addCondition($matchQuery, true);

        if ($fieldIds) {
            $matchQuery = sprintf('(%s AND search_index.attribute_id IN (%s))', $matchQuery, implode(',', $fieldIds));
        }

        $select->where($matchQuery);

        /*add By kai */
        $objectManager = \Magento\Framework\App\ObjectManager::getInstance(); // Instance of object manager
        $resource = $objectManager->get('Magento\Catalog\Model\ResourceModel\Product\Attribute\Collection');
        $attribute = $resource->getItemByColumnValue('attribute_code', 'sku');
        $attributeId = $attribute ? $attribute->getId() : 0;
        if($attributeId != 0 ){
            $matchQueryw = 'data_index like \'%' . $query->getValue() . '%\'';
            $select->orWhere($matchQueryw);
        }

        return $select;
    }
			
			
			
starting next week, i will take the  job from outside to do as freelancer (14$/hour)
So if you have any project with magento, you can contact with me, i will consider your requirement and give you the feedback in shorttime.
 

thanks.
			
			
			
			

    $originalDate = "21/03/10";

    $newDate = date("d-m-Y H:i:s", strtotime($originalDate));

			
			